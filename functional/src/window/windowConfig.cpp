#include "pch.hpp"
#include "../../include/window/windowConfig.hpp"
#include "../../include/window/window.hpp"

namespace hp_fp
{
	/*WindowConfig::WindowConfig( const unsigned width, const unsigned height, const WindowStyle windowStyle = WindowStyle::Default, const unsigned bitsPerPx = 32 ) : _width( width ), _height( height ),
		_windowStyle( windowStyle ), _bitsPerPx( bitsPerPx )
	{

	}

	WindowConfig::WindowConfig( const WindowStyle windowStyle = WindowStyle::Default ) : _width( std::get<0>( Window::getCurrentMode( ) ) ), _height( std::get<1>( Window::getCurrentMode( ) ) ), 
		_windowStyle( windowStyle ), _bitsPerPx( std::get<2>( Window::getCurrentMode( ) ) )
	{
		
	}*/
}